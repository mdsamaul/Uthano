# 🌐 নেটওয়ার্ক সেটআপ গাইড

এই গাইড অনুসরণ করে আপনি আপনার লোকাল নেটওয়ার্কে অন্য ডিভাইস থেকে UTHANO ব্যাকেন্ড এবং ফ্রন্টেন্ড অ্যাক্সেস করতে পারবেন।

## 🖥️ হোস্ট পিসি কনফিগারেশন (যেখানে সার্ভার চলছে)

**হোস্ট পিসি IP:** `192.168.137.219`

### ব্যাকেন্ড সার্ভার (পোর্ট ৮০০০)
```bash
cd backend
php artisan serve --host=0.0.0.0 --port=8000
```

### ফ্রন্টেন্ড সার্ভার (পোর্ট ৩০০০)
```bash
cd frontend
npm run dev
```

## 📱 অন্য ডিভাইস থেকে অ্যাক্সেস

### ব্যাকেন্ড API
- **URL:** `http://192.168.137.219:8000/api/v1`
- **স্ট্যাটাস:** ✅ চলছে (সব নেটওয়ার্ক থেকে অ্যাক্সেসযোগ্য)

### ফ্রন্টেন্ড 
- **URL:** `http://192.168.137.219:3000`
- **API কানেকশন:** ✅ কনফিগার্ড (`192.168.137.219:8000`)

## 🔧 ক্লায়েন্ট ডিভাইস সেটআপ

### ১. মোবাইল/ট্যাবলেট/ল্যাপটপ ব্রাউজারে
```
Backend API: http://192.168.137.219:8000/api/v1
Frontend:    http://192.168.137.219:3000
```

### ২. ডেমো ইউজার অ্যাকাউন্ট
| রোল | ইমেইল | পাসওয়ার্ড |
|-----|--------|-----------|
| Admin | `admin@uthano.com` | `password` |
| Customer | `customer@uthano.com` | `password` |
| Farmer | `farmer@uthano.com` | `password` |

## 🌍 নেটওয়ার্ক কানেকশন টেস্টিং

### API টেস্ট (যেকোনো ডিভাইস থেকে)
```bash
# Login টেস্ট
curl -X POST http://192.168.137.219:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@uthano.com","password":"password"}'

# Products টেস্ট
curl http://192.168.137.219:8000/api/v1/products
```

## ⚠️ গুরুত্বপূর্ণ নোট

1. **ফায়ারওয়াল/অ্যান্টিভাইরাস:** নেটওয়ার্ক ফায়ারওয়াল বন্ধ না থাকলে অ্যাক্সেস করুন
2. **একই নেটওয়ার্ক:** সব ডিভাইস একই Wi-Fi নেটওয়ার্কে থাকতে হবে
3. **হোস্ট পিসি:** সার্ভার চালু থাকতে হবে এবং Sleep Mode এ না যাওয়া উচিত

## 🔧 সমস্যা সমাধান

### সমস্য: "Connection refused"
- ব্যাকেন্ড সার্ভার চালু আছে কিনা চেক করুন:
```bash
# হোস্ট পিসিতে
netstat -an | findstr ":8000"
```

### সমস্য: "CORS Error"
- CORS কনফিগারেশন চেক করুন:
```bash
# backend/config/cors.php
# এবং
# backend/bootstrap/app.php
```

### সমস্য: "Network Unreachable"
- ফায়ারওয়াল চেক করুন
- একই নেটওয়ার্কে আছে কিনা নিশ্চিত করুন

## 📋 সার্ভিস স্ট্যাটাস

| সার্ভিস | URL | স্ট্যাটাস |
|---------|-----|-------|
| **Backend API** | `http://192.168.137.219:8000` | ✅ Running |
| **Frontend** | `http://192.168.137.219:3000` | ✅ Running |
| **MySQL Database** | `localhost:3306` | ✅ Connected |

---

**তৈরি করা হয়েছে:** 2026-08-14  
**হোস্ট IP:** 192.168.137.219  
**নেটওয়ার্ক:** mshome.net